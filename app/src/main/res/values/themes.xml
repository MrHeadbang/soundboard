<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Cstmsndbrd" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/primaryAccent</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:textColorPrimary">@color/primaryAccent</item>
        <item name="colorControlHighlight">@color/primaryAccent</item>

    </style>
    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/primaryAccent</item>
        <item name="android:textColor">@color/primaryAccent</item>
        <item name="android:textColorPrimary">@color/primaryAccent</item>
        <item name="android:background">@color/primary</item>
    </style>
    <style name="AlertDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:textColor">@color/primaryAccent</item>
        <item name="android:background">@color/primary</item>
        <item name="android:tint">@color/primaryAccent</item>
        <item name="android:colorAccent">@color/primaryAccent</item>
    </style>

</resources>